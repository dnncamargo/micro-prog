#include<stdio.h>
#include<stdlib.h>

int exec(int*, int*, int*, int*);

int add(int*, int*, int*);

int addi(int*, int*);

int mult(int*, int*, int*);

int and(int*, int*, int*);

int or(int*, int*, int*);

int not(int*, int*);

int shr(int*, int*);

int shl(int*, int*);
