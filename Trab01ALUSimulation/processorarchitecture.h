#include<stdio.h>
#include<stdlib.h>

#define MAXLEN 32
#define FLAGLEN 4

void chooseopt(int, int*);

void store(char, int*);

int load(char, int*);

void defoperation(int*);

void printarray(int*, int);

void resetarray(int*, int);
